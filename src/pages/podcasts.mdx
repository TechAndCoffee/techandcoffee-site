---
layout: ../layouts/Post.astro
title: Podcasts
---

import Card from "../components/Card.astro";
import podcasts from "../data/podcasts.json";

Here are some podcasts we love by some people we love. Thanks for the idea [@thelovebug](https://twitter.com/thelovebug)!

<section class="prose dark:prose-invert lg:prose-xl prose-h2:text-center max-w-full items-center">
   <div class="grid grid-cols-1 md:grid-cols-2 gap-4 w-full justify-items-center">
      {podcasts.podcasts.map((option) => (
         <Card
            href={option.link}
            title={option.name}
            body={option.body}
            image={option.icon}
            chatLink={option.chatLink}
            chatName={option.chatName}
            label="Website"
         />
      ))}
   </div>
</section>